const c=document.querySelector('#game'),g=c.getContext('2d'),$=q=>document.querySelector(q);let W,H,run=false,last=0,score=0,shards=0,speed=300,player,roofs,stars,keys={};
function size(){W=c.width=innerWidth*devicePixelRatio;H=c.height=innerHeight*devicePixelRatio;c.style.width=innerWidth+'px';c.style.height=innerHeight+'px';g.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);W=innerWidth;H=innerHeight}addEventListener('resize',size);size();
function reset(){score=0;shards=0;speed=300;player={x:W*.22,y:H*.63,vy:0,on:false,trail:[]};roofs=[{x:-100,w:W*.42,y:H*.73},{x:W*.43,w:W*.28,y:H*.62},{x:W*.81,w:W*.4,y:H*.72}];stars=Array.from({length:60},()=>({x:Math.random()*W,y:Math.random()*H*.55,r:Math.random()*1.8+.3}));run=true;$('#end').hidden=true;$('#hud').hidden=false;last=performance.now()}
$('#play').onclick=()=>{$('#menu').hidden=true;reset()};$('#again').onclick=reset;addEventListener('keydown',e=>{keys[e.code]=true;if((e.code==='Space'||e.code==='ArrowUp')&&run){e.preventDefault();jump()}});addEventListener('keyup',e=>keys[e.code]=false);c.addEventListener('pointerdown',jump);
function jump(){if(player.on){player.vy=-630;player.on=false}}function addRoof(){let last=roofs[roofs.length-1],gap=85+Math.random()*95,w=130+Math.random()*190,y=H*(.53+Math.random()*.23);roofs.push({x:last.x+last.w+gap,w,y,shard:Math.random()<.8})}
function drawCity(t){let sky=g.createLinearGradient(0,0,0,H);sky.addColorStop(0,'#101639');sky.addColorStop(.55,'#393675');sky.addColorStop(1,'#fb795c');g.fillStyle=sky;g.fillRect(0,0,W,H);g.fillStyle='#fff7cf';g.beginPath();g.arc(W*.76,H*.23,46,0,7);g.fill();g.fillStyle='#ffffffbb';stars.forEach(s=>g.fillRect(s.x,s.y,s.r,s.r));for(let i=0;i<18;i++){let x=(i*113-(score*.16)%113),h=80+(i*47)%180;g.fillStyle=i%2?'#1d2851':'#25204c';g.fillRect(x,H*.7-h,82,h);g.fillStyle='#7c75b2';for(let y=H*.7-h+16;y<H*.67;y+=20)g.fillRect(x+14,y,7,4)}}
function frame(t){requestAnimationFrame(frame);let dt=Math.min(.035,(t-last)/1000||.016);last=t;if(run){speed+=dt*2.3;score+=dt*10;player.vy+=1550*dt;player.y+=player.vy*dt;player.on=false;roofs.forEach(r=>{r.x-=speed*dt;if(player.x+20>r.x&&player.x-12<r.x+r.w&&player.vy>=0&&player.y+38>=r.y&&player.y+38<=r.y+32){player.y=r.y-38;player.vy=0;player.on=true;if(r.shard){r.shard=false;shards++}}});while(roofs.length&&roofs[0].x+roofs[0].w<0)roofs.shift();while(roofs[roofs.length-1].x<W+250)addRoof();if(player.y>H+80){run=false;$('#hud').hidden=true;$('#final').textContent=Math.floor(score)+' points · '+shards+' shards';$('#end').hidden=false}$('#score').textContent=String(Math.floor(score)).padStart(4,'0');$('#energy').textContent='✦ '+shards}drawCity(t);roofs.forEach(r=>{g.fillStyle='#141b35';g.fillRect(r.x,r.y,r.w,H-r.y);g.fillStyle='#74f3dc';g.fillRect(r.x,r.y,r.w,5);g.fillStyle='#2c3763';for(let x=r.x+12;x<r.x+r.w;x+=28)g.fillRect(x,r.y+20,12,7);if(r.shard){g.fillStyle='#fff29a';g.beginPath();g.arc(r.x+r.w*.56,r.y-18,9,0,7);g.fill();g.fillStyle='#fff9d2';g.fillRect(r.x+r.w*.56-2,r.y-35,4,34)}});if(run){player.trail.push({x:player.x-16,y:player.y+26,a:1});player.trail=player.trail.slice(-10);player.trail.forEach((q,i)=>{g.fillStyle='rgba(116,243,220,'+(i/18)+')';g.fillRect(q.x-i*5,q.y,12,5)});g.fillStyle='#ffcf8c';g.fillRect(player.x-13,player.y,26,25);g.fillStyle='#18203e';g.fillRect(player.x-16,player.y+24,12,15);g.fillRect(player.x+4,player.y+24,12,15);g.fillStyle='#5e4b9b';g.fillRect(player.x-15,player.y-7,30,10);g.fillStyle='#fff';g.fillRect(player.x+5,player.y+8,4,4)}}frame(0);
